apiVersion: v1
kind: Service
metadata:
  name: work-service
  namespace: workbee
spec:
  selector:
    app: work-service
  ports:
    - name: http
      port: 4002
      targetPort: 4002
  type: ClusterIP