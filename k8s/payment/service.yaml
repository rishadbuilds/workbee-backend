apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: workbee
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 4005
      targetPort: 4005
  type: ClusterIP