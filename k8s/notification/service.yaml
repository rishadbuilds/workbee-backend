apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: workbee
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 4004
      targetPort: 4004
  type: ClusterIP