apiVersion: v1
kind: Service
metadata:
  name: communication-service
  namespace: workbee
spec:
  selector:
    app: communication-service
  ports:
    - name: http
      port: 4003
      targetPort: 4003
  type: ClusterIP