/** 1% platform fee (same value as the old PLATFORM_FEE_PERCENT constant). */
export const PLATFORM_FEE_RATE = 0.01;

export const DEFAULT_CURRENCY = "INR";

export const MINOR_UNITS_PER_MAJOR_UNIT = 100;